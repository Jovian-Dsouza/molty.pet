services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: molty
      POSTGRES_PASSWORD: molty
      POSTGRES_DB: molty
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
